[project]
name = "voice-agent"
version = "1.0.0"
description = "Production-ready AI Voice Agent with configurable STT, TTS, LLM"
requires-python = ">=3.11"
dependencies = [
    "livekit-agents[openai,silero,deepgram,turn-detector]~=1.4",
    "livekit-plugins-elevenlabs~=1.4",
    "livekit-plugins-anthropic~=1.4",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
    "loguru>=0.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.8.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
